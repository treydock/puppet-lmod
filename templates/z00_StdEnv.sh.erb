if [ -z "${USER_IS_ROOT:-}" ]; then
  if [ -z "$__Init_Default_Modules" ]; then
    export __Init_Default_Modules=1
    export LMOD_SYSTEM_DEFAULT_MODULES="<%= scope.lookupvar('lmod::default_module') %>"
    module --initial_load restore
  else
    module refresh
  fi
fi
